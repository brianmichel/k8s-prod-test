apiVersion: v1
kind: Service
metadata:
  name: playground-api
  labels:
    app: playground-api
spec:
  type: ClusterIP
  selector:
    app: playground-api
  ports:
    - name: http
      port: 8080
      targetPort: http

