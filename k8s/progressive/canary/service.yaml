apiVersion: v1
kind: Service
metadata:
  name: playground-api-canary
  labels:
    app: playground-api-canary
spec:
  selector:
    app: playground-api-canary
  ports:
    - name: http
      port: 8080
      targetPort: http

